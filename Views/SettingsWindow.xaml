<Window x:Class="RSSReader.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置"
        Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FAFBFC">

    <Window.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="24,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1D4ED8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- GLM API Settings -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="🤖 GLM API 设置" FontSize="16" FontWeight="Bold" 
                                   Foreground="#1F2937" Margin="0,0,0,16"/>

                        <TextBlock Text="API Key" Foreground="#6B7280" Margin="0,0,0,6"/>
                        <PasswordBox x:Name="ApiKeyBox" Padding="10" 
                                     BorderBrush="#E5E7EB" BorderThickness="1"/>

                        <TextBlock Text="模型" Foreground="#6B7280" Margin="0,16,0,6"/>
                        <TextBlock Text="glm-4.7-flash" Padding="10" Foreground="#1F2937" FontWeight="Medium"/>

                        <TextBlock Text="Temperature" Foreground="#6B7280" Margin="0,16,0,6"/>
                        <Slider x:Name="TemperatureSlider" Minimum="0" Maximum="1" 
                                Value="0.7" TickFrequency="0.1" IsSnapToTickEnabled="True"/>
                        <TextBlock Text="{Binding ElementName=TemperatureSlider, Path=Value, StringFormat='{}{0:F1}'}" 
                                   HorizontalAlignment="Right" Foreground="#6B7280"/>

                        <TextBlock Text="Max Tokens" Foreground="#6B7280" Margin="0,16,0,6"/>
                        <TextBox x:Name="MaxTokensBox" Padding="10" 
                                 BorderBrush="#E5E7EB" BorderThickness="1" Text="2048"/>
                    </StackPanel>
                </Border>

                <!-- RSS Settings -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="📰 RSS 设置" FontSize="16" FontWeight="Bold" 
                                   Foreground="#1F2937" Margin="0,0,0,16"/>

                        <TextBlock Text="刷新间隔（分钟）" Foreground="#6B7280" Margin="0,0,0,6"/>
                        <TextBox x:Name="RefreshIntervalBox" Padding="10" 
                                 BorderBrush="#E5E7EB" BorderThickness="1" Text="30"/>

                        <TextBlock Text="每个订阅源最大文章数" Foreground="#6B7280" Margin="0,16,0,6"/>
                        <TextBox x:Name="MaxArticlesBox" Padding="10" 
                                 BorderBrush="#E5E7EB" BorderThickness="1" Text="100"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E5E7EB" 
                BorderThickness="0,1,0,0" Padding="16">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" Padding="24,10" Click="CancelButton_Click"
                        Background="#F3F4F6" Foreground="#1F2937" BorderThickness="0"/>
                <Button Content="保存" Style="{StaticResource PrimaryButton}" 
                        Margin="12,0,0,0" Click="SaveButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
