<UserControl x:Class="RSSReader.Views.ArticleWebViewPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E5E7EB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#FAFBFC">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Style="{StaticResource SecondaryButton}" 
                        Click="BackButton_Click" Padding="12,8">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="←" Margin="0,0,4,0"/>
                        <TextBlock Text="返回"/>
                    </StackPanel>
                </Button>

                <TextBlock Grid.Column="1" x:Name="TitleText"
                           FontSize="14" FontWeight="SemiBold" Foreground="#1F2937"
                           TextTrimming="CharacterEllipsis" VerticalAlignment="Center"
                           Margin="12,0,0,0"/>

                <Button Grid.Column="2" Style="{StaticResource SecondaryButton}" 
                        Click="OpenLinkButton_Click" Padding="12,8"
                        ToolTip="在浏览器中打开">
                    <TextBlock Text="🔗"/>
                </Button>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Background="White" Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="8,8,8,0">
                <TextBlock x:Name="ArticleTitle" 
                           FontSize="20" FontWeight="Bold" Foreground="#1F2937"
                           TextWrapping="Wrap" Margin="0,0,0,8"/>

                <StackPanel x:Name="MetaPanel" Orientation="Horizontal">
                    <TextBlock x:Name="PublishDateText" Foreground="#6B7280" FontSize="12"/>
                    <TextBlock x:Name="FeedTitleText" Foreground="#6B7280" FontSize="12" Margin="8,0,0,0"/>
                </StackPanel>
            </StackPanel>

            <!-- HTML Content -->
            <WebBrowser Grid.Row="1" x:Name="HtmlContent" 
                        Margin="0,8,0,0"/>
        </Grid>
    </Grid>
</UserControl>
